version: "3"

services:
  reverse-proxy:
    build: nginx
    depends_on:
      - emailservice
    volumes:
      - ./ngnix/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80

  emailservice:
    build: EmailService
    ports:
      - 8000:8000
    environment:
      VIRTUAL_HOST: localhost
